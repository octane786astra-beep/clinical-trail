# PostgreSQL (production)
DATABASE_URL=postgresql://username:password@localhost:5432/clinical_trial_db

# SQLite (demo - works out of the box, no setup needed)
# DATABASE_URL=sqlite:///./clinical_trial_platform.db

# Server
HOST=0.0.0.0
PORT=8000
